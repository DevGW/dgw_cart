source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in dgw_cart.gemspec.
gemspec

group :development do
  gem 'sqlite3'
  gem 'awesome_print'
  gem 'rspec'
end

# To use a debugger
# gem 'byebug', group: [:development, :test]
